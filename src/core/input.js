export class Input{ constructor(){ this.keys=new Set(); this.pressed=new Set(); this.released=new Set(); addEventListener('keydown',e=>{ if(!e.repeat) this.pressed.add(e.code); this.keys.add(e.code);}); addEventListener('keyup',e=>{ this.keys.delete(e.code); this.released.add(e.code);}); } beginFrame(){ this.pressed.clear(); this.released.clear(); } down(c){return this.keys.has(c);} justPressed(c){return this.pressed.has(c);} justReleased(c){return this.released.has(c);} }